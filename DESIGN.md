# Граббер — DESIGN.md

Загрузчик видео для Android. Система та же, что у Пульта (`../cpanel/DESIGN.md`): clean-product на
Material 3, свои токены вместо дефолтной палитры, IBM Plex, одна кривая движения. Отличается гамма —
тёплая «Персик и песок», чтобы две иконки и два приложения не путались. Светлая и тёмная темы
равноправны, по умолчанию — как в системе.

Инструмент на один жест: вставил ссылку → выбрал → оно качается. Никакой витрины.

## Палитра

| Токен | Светлая | Тёмная | Где |
|---|---|---|---|
| `bg` | `#F8F4EF` | `#1F1C1A` | фон экрана |
| `surface` | `#FFFFFF` | `#282421` | карточки, листы |
| `surfaceMuted` | `#F1EBE3` | `#322D29` | поля ввода, плашки, заглушки превью |
| `outline` | `#E3DACE` | `#433C36` | разделители, рамки |
| `text` | `#2E2823` | `#F1ECE6` | основной текст |
| `textMuted` | `#756B61` | `#B0A69C` | подписи, размеры, длительность |
| `accent` | `#F4B993` | `#F0BFA3` | главная кнопка, прогресс, выбранный формат |
| `onAccent` | `#2E2823` | `#1F1C1A` | текст на `accent` |
| `accentInk` | `#9A5433` | `#F0BFA3` | ссылки, иконки активного состояния |
| `sand` | `#E8D9BF` | `#4A4033` | второй тон: дорожка прогресса, фон ожидающих задач |

Статусы — заливка + тёмные чернила. Персик тёплый и светлый, поэтому ошибка сдвинута в холодную
розово-малиновую сторону, а предупреждение — в соломенно-жёлтую; и ни один статус не передаётся
только цветом, рядом всегда слово.

| Статус | Заливка (светл / тёмн) | Чернила (светл / тёмн) |
|---|---|---|
| ok | `#B9D8B4` / `#27382A` | `#2F6B45` / `#B9D8B4` |
| warn | `#EBDDA0` / `#3A3522` | `#6E5A12` / `#EBDDA0` |
| error | `#F2C3CB` / `#3D2429` | `#9B2F46` / `#F2C3CB` |

## Типографика

Как у Пульта. **IBM Plex Sans** для интерфейса, **IBM Plex Mono** для размеров, длительности,
процентов (табличные цифры, чтобы строка не прыгала при обновлении прогресса). Шрифты в
`assets/fonts/` с лицензиями OFL.

| Роль | Шрифт | Размер/строка | Вес |
|---|---|---|---|
| title | Plex Sans | 20/26 | SemiBold |
| subtitle (название ролика) | Plex Sans | 16/22 | Medium |
| body | Plex Sans | 15/21 | Regular |
| label | Plex Sans | 13/18 | Medium |
| caption | Plex Sans | 12/16 | Regular |
| metric (размер, %, 4:12) | Plex Mono | 13/18 | Regular, цифры табличные |

## Форма

- Сетка 4pt: 4, 8, 12, 16, 24, 32.
- Радиусы: карточки и превью 12, кнопки и поля 10, чипы форматов 8. Больше 16 — нигде.
- Тени не используем: глубина через `surface` на `bg` и `outline` 1px.
- Превью роликов — 16:9 для YouTube, 9:16 для TikTok/Reels: форма повторяет источник, а не
  подгоняется под одну ячейку.

## Композиция

- Главный экран — не сетка. Сверху поле ссылки на всю ширину. Под ним текущая загрузка крупно
  (обложка, название, прогресс, фаза). Очередь — компактными строками. Ниже история — список
  строк «обложка слева, текст справа», обложка по форме источника.
- Экран выбора: превью наверху, под ним форматы списком строк (качество · кодек · размер),
  лучший отмечен заранее. Кнопка «Скачать» внизу, закреплена.
- Карусели: сетка превью в 3 колонки — единственное место, где сетка уместна, потому что это
  и есть набор однотипных картинок.

## Моушен

- Одна кривая: `Cubic(0.22, 1, 0.36, 1)`. UI-переходы 180 мс, смена экрана 240 мс.
- Прогресс-бар едет плавно (180 мс между обновлениями), проценты просто меняются.
- `MediaQuery.disableAnimations` → все длительности в ноль.
- Не анимируем: появление строк истории, смену процентов, список форматов.

## Запреты для этого проекта

- Градиенты, glassmorphism, свечения, тени.
- Сетка равных карточек на главном экране. Логотипы платформ цветными бейджами.
- Пульсирующие индикаторы, stagger-появление, scale при нажатии, конфетти по завершении.
- Inter, Roboto, Manrope. Эмодзи в интерфейсе.
- Выдуманные цифры: размер, которого yt-dlp не сообщил, показывается как «≈» или не показывается.
- Заглушки «скоро»: если функции нет, её нет в интерфейсе.
